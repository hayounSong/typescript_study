"use strict";
const a = '5';
const b = 5;
const c = true;
const d = undefined;
const e = null;
const f = '123';
// any는 모든타입이 다되는것, ts에선 사용을 자제하자.
const t = 5;
//값 고정
const js = () => { };
function add_simple(x, y) { return x + y; }
;
const result = add_simple(1, 2);
// type alias 를 통해 type 으로 타입 선언도 가능
const add = (x, y) => x + y;
const obj = { lat: 37.5, lon: 127.5 };
const arr = ['123', '456'];
const arr2 = ['123', '456'];
//generic식 선언 방법
const tuple = [123, 456, 'helo'];
Array;
add4(x, y);
{
    return x + y;
}
